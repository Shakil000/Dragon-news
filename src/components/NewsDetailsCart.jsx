import React from 'react';

const NewsDetailsCart = () => {
    const data = useLoaderData();

    // console.log(data);
    return (
        <div>
            
        </div>
    );
};

export default NewsDetailsCart;