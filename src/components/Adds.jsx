import React from 'react';
import adds from '../assets/bg.png'
const Adds = () => {
    return (
        <div>
            <div>
                <img src={adds} alt="" />
            </div>
        </div>
    );
};

export default Adds;